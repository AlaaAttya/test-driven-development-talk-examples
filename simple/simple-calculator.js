class SimpleCalculator {

    /**
     * Add two integers
     *
     * @param  x
     * @param y
     * @return {int}
     */
    add(x, y) {
        return parseInt(x) + parseInt(y);
    }
}


module.exports = SimpleCalculator;